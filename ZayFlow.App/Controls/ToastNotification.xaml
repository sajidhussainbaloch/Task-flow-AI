<UserControl x:Class="ZayFlow.App.Controls.ToastNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ZayFlow.App.Converters">

    <UserControl.Resources>
        <converters:NotificationTypeToColorConverter x:Key="TypeColor"/>
        <converters:NotificationTypeToBgConverter x:Key="TypeBg"/>
    </UserControl.Resources>

    <Grid>
        <Border Background="{Binding Type, Converter={StaticResource TypeBg}}"
                BorderThickness="1" CornerRadius="10" Padding="14,12"
                Margin="8" MaxWidth="380" Opacity="0">
            <Border.BorderBrush>
                <SolidColorBrush Color="{Binding Type, Converter={StaticResource TypeColor}}" Opacity="0.3"/>
            </Border.BorderBrush>
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.15" Color="Black"/>
            </Border.Effect>
            <Border.RenderTransform>
                <TransformGroup>
                    <TranslateTransform x:Name="SlideTransform" X="40"/>
                    <TranslateTransform x:Name="LiftTransform" Y="-8"/>
                </TransformGroup>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="SlideTransform"
                                             Storyboard.TargetProperty="X"
                                             From="40" To="0" Duration="0:0:0.24">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetName="LiftTransform"
                                             Storyboard.TargetProperty="Y"
                                             From="-8" To="0" Duration="0:0:0.24">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut"/>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Icon -->
                <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                        Background="{Binding Type, Converter={StaticResource TypeColor}}"
                        Margin="0,0,10,0">
                    <TextBlock Text="{Binding Icon}" FontSize="14" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Content -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="13"
                               Foreground="#F8FAFC" Margin="0,0,0,2"/>
                    <TextBlock Text="{Binding Message}" FontSize="12" TextWrapping="Wrap"
                               Foreground="#E2E8F0"
                               Visibility="{Binding Message, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="2" Style="{StaticResource GhostButton}"
                        Content="✕" FontSize="12" Padding="6" Margin="6,0,0,0" Foreground="#F8FAFC"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
