<UserControl x:Class="ZayFlow.App.Views.AutomationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:ZayFlow.App.Converters">

    <UserControl.Resources>
        <converters:RiskLevelToBrushConverter x:Key="RiskBrush"/>
    </UserControl.Resources>

    <ScrollViewer Style="{StaticResource PremiumScrollViewer}" Padding="{StaticResource PagePadding}">
        <StackPanel>

            <!-- Header -->
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="Automation" Style="{StaticResource HeadingXL}" Margin="0,0,0,6"/>
                <TextBlock Text="Enter commands to automate file operations." Style="{StaticResource BodyText}"/>
            </StackPanel>

            <!-- Command Input Card -->
            <Border Style="{StaticResource PremiumCard}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Command" FontWeight="SemiBold" FontSize="13"
                               Foreground="{DynamicResource PrimaryTextBrush}" Margin="0,0,0,10"/>
                    <TextBox Style="{StaticResource PremiumTextBox}"
                             Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}"
                             AcceptsReturn="True" TextWrapping="Wrap" MinHeight="80" MaxHeight="160"
                             VerticalScrollBarVisibility="Auto"/>

                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0" HorizontalAlignment="Left">
                        <Button Style="{StaticResource PrimaryButton}" Content="Preview"
                                Command="{Binding PreviewCommand}" Margin="0,0,10,0"/>
                        <Button Style="{StaticResource SuccessButton}" Content="Execute"
                                Command="{Binding ExecuteCommand}" Margin="0,0,10,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Content="Undo"
                                Command="{Binding UndoCommand}" Margin="0,0,10,0"/>
                        <Button Style="{StaticResource GhostButton}" Content="Run Queue"
                                Command="{Binding ProcessQueueCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <TextBlock Text="Automation Builder" Style="{StaticResource HeadingMD}" Margin="0,0,0,14"/>
            <Border Style="{StaticResource PremiumCard}" Margin="0,0,0,20">
                <StackPanel>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Rule Name" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TertiaryTextBrush}" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="1" Text="File Type" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TertiaryTextBrush}" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="2" Text="Destination" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TertiaryTextBrush}" Margin="0,0,8,0"/>
                        <TextBlock Grid.Column="3" Text="Schedule" FontSize="11" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TertiaryTextBrush}"/>
                    </Grid>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Margin="0,0,8,0" Style="{StaticResource PremiumTextBox}" Text="{Binding RuleName}"/>
                        <TextBox Grid.Column="1" Margin="0,0,8,0" Style="{StaticResource PremiumTextBox}" Text="{Binding RuleFileType}"/>
                        <TextBox Grid.Column="2" Margin="0,0,8,0" Style="{StaticResource PremiumTextBox}" Text="{Binding RuleDestination}"/>
                        <ComboBox Grid.Column="3" Margin="0,0,8,0" Style="{StaticResource PremiumComboBox}" ItemsSource="{Binding Schedules}" SelectedItem="{Binding SelectedSchedule}"/>
                        <Button Grid.Column="4" Style="{StaticResource PrimaryButton}" Content="Add Rule" Command="{Binding AddRuleCommand}"/>
                    </Grid>
                    <TextBlock Text="Example rule: IF file type = image MOVE to Images folder (Daily / Weekly / Monthly)." Foreground="{DynamicResource TertiaryTextBrush}"/>
                </StackPanel>
            </Border>

            <TextBlock Text="Scheduled Rules" Style="{StaticResource HeadingMD}" Margin="0,0,0,14"/>
            <Border Style="{StaticResource PremiumCard}" Margin="0,0,0,20">
                <ItemsControl ItemsSource="{Binding Rules}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding FileType}" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                <TextBlock Grid.Column="2" Text="{Binding Schedule}" Foreground="{DynamicResource SecondaryTextBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <!-- Processing Indicator -->
            <Border Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVisibilityConverter}}"
                    Style="{StaticResource PremiumCard}" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4"
                                 Foreground="{DynamicResource AccentBrush}" Background="{DynamicResource SurfaceBrush}"
                                 BorderThickness="0" Margin="0,0,12,0"/>
                    <TextBlock Text="Processing..." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="13"/>
                </StackPanel>
            </Border>

            <!-- Preview Results -->
            <TextBlock Text="Preview Results" Style="{StaticResource HeadingMD}" Margin="0,0,0,14"/>
            <Border Style="{StaticResource PremiumCard}" MinHeight="200" Padding="0">
                <ItemsControl ItemsSource="{Binding PreviewItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="16,12" BorderThickness="0,0,0,1"
                                    BorderBrush="{DynamicResource DividerBrush}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource CardHoverBackgroundBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Risk Badge -->
                                    <Border Grid.Column="0" CornerRadius="12" Padding="8,3"
                                            HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Background="{Binding RiskLevel, Converter={StaticResource RiskBrush}}"
                                            Opacity="0.2"/>
                                    <TextBlock Grid.Column="0" Text="{Binding RiskLevel}"
                                               FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Left"
                                               VerticalAlignment="Center"
                                               Foreground="{Binding RiskLevel, Converter={StaticResource RiskBrush}}"/>

                                    <!-- Action Text -->
                                    <TextBlock Grid.Column="1" Text="{Binding Text}" FontSize="13"
                                               Foreground="{DynamicResource PrimaryTextBrush}"
                                               TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
